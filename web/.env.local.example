# Origin of the FastAPI backend that Next.js proxies /api/* to.
# Local dev:  http://127.0.0.1:8080
# Docker:     http://backend:8080  (set in docker-compose.yml)
BACKEND_ORIGIN=http://127.0.0.1:8080
